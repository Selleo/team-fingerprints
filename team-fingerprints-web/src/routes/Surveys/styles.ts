import { createStyles } from "@mantine/core";

export const useStyles = createStyles((theme) => ({
  addButton: {
    flex: 0,
    margin: "20px",
  },
  header: {
    display: "flex",
  },
  headerTitle: {
    flex: 1,
  },
}));
